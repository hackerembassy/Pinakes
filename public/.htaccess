# PHP upload limits (required for Digital Library plugin: eBook up to 100MB, audiobook up to 500MB)
<IfModule mod_php.c>
    php_value upload_max_filesize 512M
    php_value post_max_size 520M
    php_value max_execution_time 300
    php_value max_input_time 300
</IfModule>
<IfModule mod_php7.c>
    php_value upload_max_filesize 512M
    php_value post_max_size 520M
    php_value max_execution_time 300
    php_value max_input_time 300
</IfModule>
<IfModule mod_rewrite.c>
    Options -Indexes +FollowSymLinks
    RewriteEngine On

    # Handle Authorization Header
    RewriteCond %{HTTP:Authorization} .
    RewriteRule .* - [E=HTTP_AUTHORIZATION:%{HTTP:Authorization}]

    # Allow direct access to static files in /uploads/ directory
    RewriteCond %{REQUEST_URI} ^/uploads/ [NC]
    RewriteRule ^ - [L]

    # Redirect all requests to index.php
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^ index.php [QSA,L]
</IfModule>

# === Pinakes performance block (auto-managed by Pinakes 0.7.13+) ===
# HTTP compression + long-term caching for static assets.
# Asset URLs are version-busted via ?v=X.Y.Z (see HtmlHelper::asset), so
# cache invalidation happens automatically at every release — immutable+1y
# is safe for fingerprinted assets.
# Every directive is gated by <IfModule ...> so the file stays valid on
# hosts that don't ship mod_deflate / mod_expires / mod_headers / mod_brotli.

<IfModule mod_deflate.c>
    AddOutputFilterByType DEFLATE text/html text/plain text/css text/xml \
        text/javascript application/javascript application/x-javascript \
        application/json application/ld+json application/xml application/rss+xml \
        application/atom+xml application/xhtml+xml \
        image/svg+xml font/ttf font/otf font/woff
    # Don't re-compress already-compressed payloads
    SetEnvIfNoCase Request_URI \.(?:gif|jpe?g|png|webp|ico|woff2?|gz|zip|epub|mp4|webm|mp3|m4a|ogg)$ no-gzip dont-vary
</IfModule>

<IfModule mod_brotli.c>
    AddOutputFilterByType BROTLI_COMPRESS text/html text/plain text/css \
        text/javascript application/javascript application/json \
        application/ld+json application/xml image/svg+xml font/ttf font/otf
</IfModule>

<IfModule mod_expires.c>
    ExpiresActive On
    # 1 year for versioned static assets
    ExpiresByType text/css                "access plus 1 year"
    ExpiresByType application/javascript  "access plus 1 year"
    ExpiresByType text/javascript         "access plus 1 year"
    ExpiresByType font/woff2              "access plus 1 year"
    ExpiresByType font/woff               "access plus 1 year"
    ExpiresByType font/ttf                "access plus 1 year"
    ExpiresByType image/x-icon            "access plus 1 year"
    # 1 month for content images (book covers, uploaded media)
    ExpiresByType image/jpeg              "access plus 1 month"
    ExpiresByType image/png               "access plus 1 month"
    ExpiresByType image/webp              "access plus 1 month"
    ExpiresByType image/gif               "access plus 1 month"
    ExpiresByType image/svg+xml           "access plus 1 month"
</IfModule>

<IfModule mod_headers.c>
    # Mark versioned assets as immutable — browsers skip revalidation until TTL.
    # ?v= querystring busts the cache on every release, so this is safe.
    <FilesMatch "\.(css|js|woff2?|ttf|otf|eot|ico)$">
        Header set Cache-Control "public, max-age=31536000, immutable"
    </FilesMatch>
    <FilesMatch "\.(jpe?g|png|webp|gif|svg)$">
        Header set Cache-Control "public, max-age=2592000"
    </FilesMatch>
</IfModule>
# === end Pinakes performance block ===

# Security Headers
<IfModule mod_headers.c>
    # Prevent MIME-type sniffing
    Header always set X-Content-Type-Options "nosniff"

    # Enable XSS protection
    Header always set X-XSS-Protection "1; mode=block"

    # Prevent clickjacking
    Header always set X-Frame-Options "SAMEORIGIN"

    # Content Security Policy - restrictive but allows inline styles/scripts for compatibility
    # Allows Google Fonts for Inter font family
    Header always set Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-inline' https://cdnjs.cloudflare.com; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com https://cdnjs.cloudflare.com; font-src 'self' data: https://fonts.gstatic.com https://cdnjs.cloudflare.com; img-src 'self' data: https: http: blob:; connect-src 'self'; frame-src 'self' data: blob: about: https://www.openstreetmap.org; child-src 'self' data: blob: about:; frame-ancestors 'self';"

    # Referrer policy
    Header always set Referrer-Policy "strict-origin-when-cross-origin"
</IfModule>

# Block access to sensitive files (defense in depth)
# Even though these files are outside webroot, this prevents misconfiguration attacks
<FilesMatch "^(\.env|\.env\..+|\.git.*|\.htaccess|\.user\.ini|composer\.(json|lock)|package(-lock)?\.json|\.installed|version\.json|CLAUDE\.md)$">
    <IfModule mod_authz_core.c>
        Require all denied
    </IfModule>
    <IfModule !mod_authz_core.c>
        Order deny,allow
        Deny from all
    </IfModule>
</FilesMatch>

# Audio/Video Streaming Support (for Digital Library plugin)
<IfModule mod_headers.c>
    # Enable Range Requests for audio/video streaming
    # Required for seek functionality in audio players
    <FilesMatch "\.(mp3|m4a|ogg|wav|mp4|webm|ogv)$">
        Header set Accept-Ranges bytes
        # Allow CORS for audio/video files (required for cross-origin playback)
        Header set Access-Control-Allow-Origin "*"
        Header set Access-Control-Allow-Methods "GET, OPTIONS"
        Header set Access-Control-Allow-Headers "Range, Content-Type"
        Header set Access-Control-Expose-Headers "Content-Length, Content-Range, Accept-Ranges"
    </FilesMatch>

    # CORS for digital content in uploads/digital/ directory
    <If "%{REQUEST_URI} =~ m#^/uploads/digital/#">
        Header set Access-Control-Allow-Origin "*"
        Header set Access-Control-Allow-Methods "GET, OPTIONS"
    </If>
</IfModule>

# Correct MIME types for audio files
<IfModule mod_mime.c>
    AddType audio/mpeg .mp3
    AddType audio/mp4 .m4a
    AddType audio/ogg .ogg
    AddType audio/wav .wav
    AddType audio/webm .weba
    AddType video/mp4 .mp4
    AddType video/webm .webm
    AddType video/ogg .ogv
    AddType application/pdf .pdf
    AddType application/epub+zip .epub
</IfModule>
